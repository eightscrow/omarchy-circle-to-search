# Circle to Search — default Hyprland keybindings
# Installed by: omarchy-circle-to-search (AUR)
# Edit this file to change keybindings, or remove the
# source line from your Hyprland config to disable.

bindd = SUPER ALT, C, Circle to Search, exec, circle-to-search

# Optional: add a translate keybind (requires Ollama)
# bindd = SUPER ALT, T, Circle to Search Translate, exec, circle-to-search --translate
